#!/usr/bin/env bash
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BIN="${DIR}/opsec-engine"
if [[ -x "${BIN}" ]]; then
    exec "${BIN}" --status
else
    echo '{"text":"OPSEC: READY","tooltip":"Ready to scrub metadata","class":"normal"}'
fi
